# Analyze kernel size 1 results
python src/probing_results/analyze_time_and_layers.py \
    --pattern _ln_conv_zeros_ \
    --results_dir probing/results_csvs \
    --output_dir probing/analysis_figures

# Detect loss problems
python src/probing_results/detect_loss_problems.py \
    --pattern sana \
    --results_dir probing/results_csvs \
    --output_csv probing/loss_problems_sana.csv \
    --variance_threshold 0.01 \
    --spike_threshold 0.1 \
    --plot_graphs \
    --plot_dir probing/loss_graphs \
    --num_plot_samples 10 



# Create final representation figure for sana pattern, kernel size 1 (with baseline lines)
python src/probing_results/create_figures.py \
    --results_dir probing/results_csvs \
    --output_dir probing/figures \
    --pattern sana \
    --kernel_size 1

# Create final representation figure WITH initial curves (option 2)
python src/probing_results/create_figures.py \
    --results_dir probing/results_csvs \
    --output_dir probing/figures \
    --pattern sana \
    --kernel_size 1 \
    --include_initial_curves

# Create timestep across layers figure (show all timesteps)
python src/probing_results/create_figures.py \
    --results_dir probing/results_csvs \
    --output_dir probing/figures \
    --pattern sana \
    --kernel_size 1 \
    --mode timesteps \
    --timestep_step 1 \
    --smooth


# Create timestep across layers figure (show every 3th timestep by index)
python src/probing_results/create_figures.py \
    --results_dir probing/results_csvs \
    --output_dir probing/figures \
    --pattern sana \
    --kernel_size 1 \
    --mode timesteps \
    --timestep_step 43 \
    --smooth

# Create timestep across layers figure (start at index 1, show every 2nd timestep: indices 1,3,5,7...)
python src/probing_results/create_figures.py \
    --results_dir probing/results_csvs \
    --output_dir probing/figures \
    --pattern sana \
    --kernel_size 1 \
    --mode timesteps \
    --timestep_step 2 \
    --start_timestep_index 1 \
    --smooth